# pages/rendering

조건부 렌더링 예제를 실제로 눌러볼 수 있는 페이지입니다.

- `RenderingPage.jsx`: 로그인/로그아웃 상태(`flag`)를 `useState` 로 갖고, `components/rendering/Greeting.jsx` 에 `flag` 를 내려보내 `UserGreeting`/`GuestGreeting` 중 하나가 그려지도록 합니다. 실제 조건부 렌더링 예제들은 `components/rendering/` 폴더를 참고하세요.
