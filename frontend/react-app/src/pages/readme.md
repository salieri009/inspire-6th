# pages

이 폴더는 라우팅 단위의 화면을 두는 곳입니다.

보통 한 페이지는 하나의 URL 또는 화면 진입점에 대응합니다.

예를 들면 다음과 같습니다.

- 홈 화면
- 목록 화면
- 상세 화면
- 로그인 화면

페이지 컴포넌트는 주로 화면의 큰 구조를 담당하고, 세부 UI는 `components` 나 `features` 로 분리하는 방식이 좋습니다.