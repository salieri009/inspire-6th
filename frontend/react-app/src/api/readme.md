# api

이 폴더는 백엔드 서버와 통신하는 코드가 들어가는 곳입니다.

보통 다음과 같은 파일을 둡니다.

- 공통 API 클라이언트 설정
- fetch 또는 axios 기반 요청 함수
- 인증 토큰 처리
- 도메인별 API 모듈

화면 컴포넌트 안에 직접 요청 로직을 넣기보다, 이 폴더로 분리하면 유지보수가 쉬워집니다.


여기는 Axios 를 활용할 것임

```
npm install axios
```