
const Book = () => {
    return (
        <div>
            <div>
                <span>카테고리 : 000</span>
            </div>
        </div>
    );
}

export default Book;


//===============================================================================
// React 환경 구성 및 컴포넌트 이해 (학습 목차)
//===============================================================================
// - 프로젝트 구조 이해
// - CRA 와 Vite(TypeScript) 빌드 비교
// - 컴포넌트의 개념
// - Props 를 활용한 데이터 전달
// - 컴포넌트 합성 방법
// - 이벤트 처리 방법
// - 내부 스타일 적용 방법
// - Styled Components 활용   -> src/components/styled/Button.jsx 참고

// React 개발 환경을 구성하고, 핵심 개념인 컴포넌트와 Props 를 중심으로 학습한다.

// 프로젝트는 항상 보수적으로 생각해야 한다.
// 늘 한 가지 경우만 있는 게 아니라, 여러 상황을 고려해서 개발해야 한다.
// TypeScript 를 사용하면 타입을 지정해서 개발할 수 있다.
// (JavaScript 만 쓰는 환경이 전부는 아니다)

//===============================================================================
// 컴포넌트 합성 (composition)
//===============================================================================
// 위 Book 은 아직 값이 고정되어 있다. 다음 단계는 props 로 값을 받는 것이다.
//   const Book = ({ category }) => ... <span>카테고리 : {category}</span>
//
// 그리고 children 을 쓰면 컴포넌트 "안쪽"을 통째로 받을 수 있다.
//   const Card = ({ children }) => <div className="card">{children}</div>
//   <Card><Book category="Science" /></Card>
// 상속(extends)이 아니라 이런 합성으로 재사용하는 것이 React 의 기본 방식이다.