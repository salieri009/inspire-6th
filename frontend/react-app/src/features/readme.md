# features

이 폴더는 화면이나 도메인 단위로 묶이는 기능을 넣는 곳입니다.

예를 들면 다음과 같은 코드가 들어갑니다.

- 로그인, 회원가입, 마이페이지처럼 특정 목적이 있는 기능
- 목록 조회, 상세 조회, 등록, 수정처럼 한 흐름으로 동작하는 기능
- 관련 컴포넌트, 상태 관리, API 호출 로직을 함께 묶은 코드

기능이 커질수록 `components` 나 `pages` 와 분리해서 관리하면 구조를 이해하기 쉬워집니다.