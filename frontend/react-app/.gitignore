# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production build - 빌드 산출물은 커밋하지 않는다.
# 소스에서 언제든 다시 만들 수 있고, 커밋하면 매번 diff 가 지저분해진다.
/build

# environment
# .env 는 로컬 설정/비밀값이 들어가므로 커밋하지 않는다.
# 대신 .env.example 을 커밋해서 "어떤 키가 필요한지"만 공유한다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / os
.DS_Store
Thumbs.db
*.swp
